name: mytonprovider-frontend

services:
  frontend:
    image: ${FRONTEND_IMAGE}
    pull_policy: always
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:80"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
